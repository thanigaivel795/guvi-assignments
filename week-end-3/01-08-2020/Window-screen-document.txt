
Window:-

Window is the main JavaScript object root, aka the global object in a browser, also can be treated as the root of the document 
object model. You can access it as window

Screen:-

window.screen or just screen is a small information object about physical screen dimensions.


Docucment
window.document or just document is the main object of the potentially visible (or better yet: rendered) document object 
model/DOM.